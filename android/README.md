# Carr Hill Trading Android

Android wrapper for the Carr Hill Trading web app.

## Build
Open the `android` folder in Android Studio, allow Gradle to sync, then use **Build > Build APK(s)**.

The app loads the live GitHub Pages version so website updates are reflected in the Android app. Existing Supabase accounts/backend are shared with the web version.
